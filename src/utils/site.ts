/**
 * The site's canonical address, as in the canonical tags in index.html and
 * in public/sitemap.xml.
 *
 * Build share links and embed code from this, not from window.location: the
 * pages are pre-rendered, where there is no window, and a reader without
 * JavaScript gets the pre-rendered link as it is.
 */
export const SITE_URL = 'https://global-anti-ccp-resistance-hub.stane203.workers.dev';
