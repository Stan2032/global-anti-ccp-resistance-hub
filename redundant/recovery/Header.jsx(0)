import { useState } from 'react';
import { Menu, X } from 'lucide-react';
import { Button } from '@/components/ui/button';

const Header = () => {
  const [isMenuOpen, setIsMenuOpen] = useState(false);

  const toggleMenu = () => {
    setIsMenuOpen(!isMenuOpen);
  };

  return (
    <header className="bg-primary text-primary-foreground py-4 sticky top-0 z-50 shadow-md">
      <div className="container mx-auto px-4 flex justify-between items-center">
        <div className="flex items-center">
          <h1 className="text-xl md:text-2xl font-bold">The Digital Dragon</h1>
        </div>
        
        {/* Mobile menu button */}
        <div className="md:hidden">
          <Button 
            variant="ghost" 
            size="icon" 
            onClick={toggleMenu}
            className="text-primary-foreground hover:bg-primary-foreground/10"
          >
            {isMenuOpen ? <X size={24} /> : <Menu size={24} />}
          </Button>
        </div>
        
        {/* Desktop navigation */}
        <nav className="hidden md:flex space-x-6">
          <a href="#introduction" className="hover:underline">Introduction</a>
          <a href="#background" className="hover:underline">Background</a>
          <a href="#scale" className="hover:underline">Scale & Presence</a>
          <a href="#techniques" className="hover:underline">Techniques</a>
          <a href="#narratives" className="hover:underline">Narratives & Debunks</a>
          <a href="#influence" className="hover:underline">Influence</a>
          <a href="#countermeasures" className="hover:underline">Countermeasures</a>
        </nav>
      </div>
      
      {/* Mobile navigation */}
      {isMenuOpen && (
        <div className="md:hidden bg-primary border-t border-primary-foreground/20 py-2">
          <nav className="container mx-auto px-4 flex flex-col space-y-3">
            <a href="#introduction" className="hover:underline py-1" onClick={toggleMenu}>Introduction</a>
            <a href="#background" className="hover:underline py-1" onClick={toggleMenu}>Background</a>
            <a href="#scale" className="hover:underline py-1" onClick={toggleMenu}>Scale & Presence</a>
            <a href="#techniques" className="hover:underline py-1" onClick={toggleMenu}>Techniques</a>
            <a href="#narratives" className="hover:underline py-1" onClick={toggleMenu}>Narratives & Debunks</a>
            <a href="#influence" className="hover:underline py-1" onClick={toggleMenu}>Influence</a>
            <a href="#countermeasures" className="hover:underline py-1" onClick={toggleMenu}>Countermeasures</a>
          </nav>
        </div>
      )}
    </header>
  );
};

export default Header;

